services:
  - type: web
    name: sports-predictor-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FRONTEND_ORIGIN
        sync: false
      - key: DATABASE_PATH
        value: /opt/render/project/src/data/predictions.db
      - key: ENABLE_BACKGROUND_REFRESH
        value: "true"
      - key: FIXTURE_REFRESH_SECONDS
        value: "300"
